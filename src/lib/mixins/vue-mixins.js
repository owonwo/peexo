import {
  trace,
  logError,
  log,
  debug,
} from '../functions'

export {
  trace,
  logError,
  debug,
}
  from '../functions'

export const DebugHelpers = {
  methods: {
    trace,
    log,
    logError,
    debug,
  },
}
